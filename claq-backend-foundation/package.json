{
  "name": "claq-backend",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=22 <23" },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src infra --ext .ts",
    "test": "vitest run",
    "synth": "cdk synth",
    "deploy:demo": "cdk deploy --all --context env=demo --require-approval broadening",
    "deploy:prod": "cdk deploy --all --context env=prod --require-approval broadening"
  },
  "dependencies": { "@aws-sdk/client-s3": "^3.800.0", "@aws-sdk/s3-request-presigner": "^3.800.0", "@prisma/client": "^6.10.0", "decimal.js": "^10.5.0" },
  "devDependencies": { "@types/aws-lambda": "^8.10.150", "@types/node": "^22.15.30", "aws-cdk-lib": "^2.1000.0", "constructs": "^10.4.2", "esbuild": "^0.25.5", "prisma": "^6.10.0", "tsx": "^4.19.4", "typescript": "^5.8.3", "vitest": "^3.2.2" }
}
